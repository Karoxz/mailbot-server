fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
sqlalchemy==2.0.30
aiosqlite==0.20.0
python-jose[cryptography]==3.3.0
httpx==0.27.0
requests==2.31.0
anthropic>=0.34.0

# LLM calls (rate extraction, reply classification, broker-note
# extraction) go through Groq's free-tier API now (2026-09-08,
# replaced Gemini — see llm_client.py) via plain `requests` (already
# listed above) — no SDK package needed at all, unlike the old
# `google-genai` dependency this replaces.

# Gmail API access (standalone-engine, added 2026-09-05) — versions
# pinned to match the desktop client's venv311 (the proven-working
# reference implementation for the same OAuth/Gmail calls).
google-api-python-client==2.196.0
google-auth==2.53.0
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0
httplib2==0.31.2

# Offline US ZIP -> lat/lon lookup (added 2026-09-09, see zip_geocode.py)
# — replaces a confirmed 7-8s-per-message network geocoding bottleneck
# (nominatim.openstreetmap.org / photon.komoot.io, both public rate-
# limited services) with a local dataset lookup for the common
# "location includes a 5-digit ZIP" case. pandas/numpy come in as
# pgeocode's own dependencies — left unpinned so pip resolves versions
# actually compatible with the server's Python, same as this file
# already does for every other package's transitive deps.
pgeocode==0.5.0